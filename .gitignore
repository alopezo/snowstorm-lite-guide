# Your real env file: it holds credentials, do NOT commit it.
snowstorm-lite.env
snowstorm-lite.env.old

# RF2 zips cached from the volume via `docker cp ... ./rf2-cache`.
rf2-cache/

# Loose RF2 files left alongside for Option B.
*.zip
