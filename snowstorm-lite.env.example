# Snowstorm Lite — archivo de entorno (PLANTILLA)
#
# 1. Copiá este archivo a:  snowstorm-lite.env   (misma carpeta que docker-compose.yml)
# 2. Reemplazá los valores de ejemplo de abajo.
# 3. Corré:  docker compose up -d
#
# NOTA: mantené privado el snowstorm-lite.env real — tiene credenciales.
#       NO lo subas a un repo ni lo compartas en un gist.

# --- Cuenta admin (requerida) -------------------------------------------------
# Credenciales del login admin usado por el dashboard y la API admin.
# El usuario es "admin" si se omite; poné siempre tu propia contraseña.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=cambiame

# --- Sindicación MLDS (opcional) ----------------------------------------------
# Solo hace falta si querés que Snowstorm Lite descargue automáticamente una edición
# SNOMED CT desde el servicio MLDS de SNOMED International (https://mlds.ihtsdotools.org).
# Dejalas en blanco si vas a importar un paquete RF2 a mano.
#
# OJO: los valores se toman LITERALES (docker no procesa el archivo con la shell).
#   - NO pongas comillas alrededor del valor.
#   - NO escapes caracteres especiales con "\". Si tu contraseña es  ab*c$d  escribí
#     exactamente  ab*c$d  (no  ab\*c\$d ). Un "\" de más rompe la credencial y la
#     descarga del ZIP falla con 401.
SYNDICATION_USERNAME=tu-usuario-mlds
SYNDICATION_PASSWORD=tu-password-mlds
